# Internes Archiv – ausgelagert nach: Claude Projekte/Bürokram/_archiv/

# macOS
.DS_Store

# Node
node_modules/
promo/node_modules/

# Lokale Arbeitsdateien / Render-Scripts
make-logo.js
make-og-image.js
logo-square.html
og-image.html
render-*.js

# Promo Videos (zu groß für GitHub)
promo/output/
promo/frames/
promo/recordings/

# Interne Recherche-Dokumente / Strategie-Notizen (nicht öffentlich)
amazon-produktliste.md
*-recherche.md
*-strategie.md
*-notes.md

# CozyVibe Live-Gang: Arbeitsdateien, Build-Skripte, Test-Screenshots (nicht fürs Live-Repo)
_*
preview/cozy-picks/

# Große Bildschirmaufnahmen + Original-Kampagne-PDF (lokal)
ScreenRecording*.MP4
ScreenRecording*.mov
*.MP4
Kampagne Sommer 2026*.pdf

# Amazon PA-API Schlüssel (geheim, nie committen)
.amazon-paapi.env

# Interne Doku / Prüfberichte (nur lokal, nicht ins öffentliche Repo)
00_Memo.md
PRUEFBERICHT_*.html
PRUEF-PROMPT_*.md

# ── Ergänzt 21.08.2026 ──────────────────────────────────────────────
# ACHTUNG: Dieser Ordner IST die Live-Seite (robschman.github.io →
# beautyroutine.app). Alles was hier eingecheckt wird, ist danach
# öffentlich abrufbar. Deshalb: niemals "git add ." verwenden,
# sondern immer einzelne Dateien gezielt hinzufügen.

# Promo-Rohmaterial (zusammen rund 1,2 GB, gehört nicht auf eine Website)
promo/KI-Presenterin/
promo/Themen/
promo/musik/
Promotion/

# HINWEIS zu kampagne-sommer-2026/: NICHT ausgeschlossen, weil dort bereits
# zwei Dateien eingecheckt sind (Postplan-und-Texte.html und .pdf). Sie sind
# damit oeffentlich unter beautyroutine.app abrufbar. Falls das nicht gewollt
# ist: erst "git rm --cached" auf die beiden, dann hier ausschliessen.

# Medien in Kleinschreibung (oben stand nur *.MP4 gross)
*.mp4
*.mov
*.m4a
*.wav
*.avi

# Lose Arbeitsdateien
fetch-amazon-images.js
CozyVibe-Profilbild.png

# Vorschau der Rechtsseiten (erzeugt von Claude Projekte/Rechtstexte/_System/einbau/beauty-routine/vorschau-bauen.py, nicht auf die Live-Seite)
Rechtstexte-Vorschau.html
Rechtstexte-Vorschau-CozyVibe.html
